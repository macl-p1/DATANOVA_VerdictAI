# Implementation notes

The supplied workspace contained no frontend, backend, dataset, AWS code, schemas, prompts, or rule engine to integrate with. This standalone ML repository therefore provides an explicit OCR-text boundary and a sample deterministic rule interface. A statute table and production AWS adapter require authoritative application inputs and are deliberately not guessed.
