# Data handling

Place canonical annotation JSON files in `data/annotations/`; raw legal documents stay in `data/raw/` and are ignored by Git. Do not commit identifiable documents. Synthetic documents must use `source_type: synthetic`.
