project: verdictai-ml
dataset:
  raw_dir: data/raw
  processed_dir: data/processed
  annotations_dir: data/annotations
  version: v1.0
  max_document_characters: 250000
model:
  # Verified against the Hugging Face model card on 2026-09-19.
  name: ai4bharat/IndicBERTv2-MLM-only
  revision: e726058
  task: token-classification
data:
  max_length: 512
  stride: 128
  lowercase: false
confidence:
  high: 0.90
  medium: 0.70
  review_below: 0.70
